<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Opencv" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="AppTheme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- 如果需要透明状态栏 -->
        <item name="android:navigationBarColor">@android:color/transparent</item> <!-- 如果需要透明导航栏 -->
    </style>
</resources>

